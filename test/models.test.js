var expect = require('expect.js');
//var fs = require('fs');
var models = require('auto-loader').load(__dirname + '/../models');

describe('Models', function(){
	describe('User', function(){
		it('should be testing User', function(){
			//expect(1).to.be(1);
		});
	});
	describe('BulkPackage', function(){
		it('should be testing BulkPackage', function(){
			//expect(1).to.be(1);
		});
	});
	describe('Client', function(){
		it('should be testing Client', function(){
			//expect(1).to.be(1);
		});
	});
	describe('AccessToken', function(){
		it('should be testing AccessToken', function(){
			//expect(1).to.be(1);
		});
	});
	describe('Product', function(){
		it('should be testing Product', function(){
			//expect(1).to.be(1);
		});
	});
});